generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Meta {
  id          Int      @id @default(autoincrement())
  analista    String
  mesAno      String
  semana      String
  atividade   String
  meta        Float
  realizado   Float
  status      String?
  observacao  String?
  dataCriacao DateTime @default(now())
}
